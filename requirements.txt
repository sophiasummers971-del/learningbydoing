# Python dependencies for hackingtool
# boxes and lolcat are system CLI tools, not pip packages — install via aptpip /brew
# flask is unused — removed
# requests is unused at runtime — removed
rich>=13.0.0
